# 🚀 Дастури насб - CRM Донишгоҳи Рақамикунонӣ 2025

## Шарти зарурӣ

- Python 3.11+
- pip
- virtualenv (тавсия мешавад)

## Қадамҳои насб

### 1. Кушодани лоиҳа

```bash
unzip university_crm_final.zip
cd university_crm_final
```

### 2. Эҷоди virtual environment

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate/.
```

**Windows:**
```cmd
python -m venv venv
venv\Scripts\activate
```

### 3. Насби китобхонаҳо

```bash
pip install -r requirements.txt
```

### 4. Омодасозии базаи додаҳо

```bash
python manage.py makemigrations
python manage.py migrate
```

### 5. Эҷоди маълумоти тестӣ

```bash
python manage.py createinitialdata
```

Ин фармон автоматикан эҷод мекунад:
- ✅ 4 корбар (dean, admin, zamdekan, teacher1)
- ✅ 4 курс (1-4)
- ✅ 4 гурӯҳ
- ✅ 6 фан
- ✅ 30 донишҷӯ
- ✅ Расписание
- ✅ Журналҳо бо маълумот
- ✅ 1 семестри фаъол

### 6. Оғози сервер

```bash
python manage.py runserver
```

Кушоед браузер: **http://127.0.0.1:8000/**

## 🔐 Маълумот барои вуруд

| Нақш     | Username   | Password     |
|----------|------------|--------------|
| Декан    | dean       | dean2025     |
| Админ    | admin      | admin2025    |
| Замдекан | zamdekan   | zam2025      |
| Омӯзгор  | teacher1   | teacher2025  |

## ✅ Санҷиши кор

1. Вуруд бо `dean` / `dean2025`
2. Дидани Live Dashboard
3. Санҷиши рӯйхати донишҷӯён
4. Экспорти ҳисобот ба PDF

## 🛠 Мушкилот

### Хатои миграсия

```bash
python manage.py migrate --run-syncdb
```

### Модулҳо ёфт намешаванд

```bash
pip install --upgrade pip
pip install -r requirements.txt --force-reinstall
```

### Порти банд аст

```bash
python manage.py runserver 8001
```

## 📦 Истифода дар продукшн

1. Тағйир додани SECRET_KEY дар settings.py
2. DEBUG = False
3. Иловаи ALLOWED_HOSTS
4. Истифодаи PostgreSQL/MySQL
5. Ҷамъовардани static: `python manage.py collectstatic`

## 🎯 Амалиёти минбаъда

Пас аз насб метавонед:
- Эҷоди корбарони нав
- Иловаи донишҷӯён
- Импорти журнал аз Excel
- Таҳлили статистика
- Экспорти ҳисоботҳо

---
**Муваффақ бошед!** 🎓
